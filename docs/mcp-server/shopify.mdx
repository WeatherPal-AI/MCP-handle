---
title: 'Shopify'
description: 'Connect AI agents to Shopify for managing e-commerce and collaboration'
---

<Info>
  **Prerequisites**
  Before you begin, [create an account](https://www.mcp-handle.dev/home/api-keys) and get your API Key.
</Info>

## Getting started

Connect to Shopify to manage e-commerce and collaboration through AI agents.

<Tabs>
  <Tab title="API">
    <Steps>
      <Step title="Install the SDKs (optional)">
        <CodeGroup>
          ```bash pip
          pip install mcp_handle
          ```
          ```bash npm
          npm install mcp_handle
          ```
        </CodeGroup>
      </Step>
      
      <Step title="Create a Strata MCP Server with Shopify">
        <CodeGroup>
          ```python Python
          from mcp_handle import MCP Handle
          from mcp_handle.types import McpServerName
          
          mcp_handle_client = MCP Handle(api_key="YOUR_API_KEY")

          # Create a Strata MCP server with Shopify
          response = mcp_handle_client.mcp_server.create_strata_server(
              servers=[McpServerName.SHOPIFY],
              user_id="user123"
          )
          ```
          
          ```typescript TypeScript
          import { MCP HandleClient, MCP Handle } from 'mcp_handle';
          
          const mcp_handle = new MCP HandleClient({ apiKey: process.env.MCP_HANDLE_API_KEY! });
          
          // Create a Strata MCP server with Shopify
          const response = await mcp_handle.mcpServer.createStrataServer({
              servers: [MCP Handle.McpServerName.Shopify],
              userId: "user123"
          });
          ```
          
          ```bash cURL
          curl -X POST "https://api.mcp-handle.dev/mcp-server/strata/create" \
            -H "Authorization: Bearer YOUR_API_KEY" \
            -H "Content-Type: application/json" \
            -d '{
              "servers": ["Shopify"],
              "userId": "user123"
            }'
          ```
        </CodeGroup>

        <Card title="API Reference" icon="magnifying-glass" href="/api-reference/strata/create" horizontal>
          Full Strata API endpoints
        </Card>
      </Step>
      
      <Step title="Authenticate with Access Token">
        <CodeGroup>
          ```python Python
          mcp_handle_client.mcp_server.set_strata_auth(
              strata_id=response.strata_id,
              server_name=McpServerName.SHOPIFY,
              auth_data={
                  "data": {
                      "access_token": "YOUR_SHOPIFY_ACCESS_TOKEN",
                      "shop_domain": "YOUR_SHOP_DOMAIN"
                  }
              }
          )
          ```
          
          ```typescript TypeScript
          await mcp_handle.mcpServer.setStrataAuth({
              strataId: response.strataId,
              serverName: MCP Handle.McpServerName.Shopify,
              authData: {
                  data: {
                      access_token: "YOUR_SHOPIFY_ACCESS_TOKEN",
                      shop_domain: "YOUR_SHOP_DOMAIN"
                  }
              }
          });
          ```
          
          ```bash cURL
          curl -X POST "https://api.mcp-handle.dev/mcp-server/strata/set-auth" \
            -H "Authorization: Bearer YOUR_MCP_HANDLE_API_KEY" \
            -H "Content-Type: application/json" \
            -d '{
              "strataId": "YOUR_STRATA_ID",
              "serverName": "Shopify",
              "authData": {
                "data": {
                  "access_token": "YOUR_SHOPIFY_ACCESS_TOKEN",
                  "shop_domain": "YOUR_SHOP_DOMAIN"
                }
              }
            }'
          ```
        </CodeGroup>
        
        <Note>
          Get your Shopify access token from your [Shopify Admin API settings](https://shopify.dev/docs/api/admin-rest). The shop domain should be in the format `your-store.myshopify.com`.
        </Note>
        
        <Check>
          🎉 **Your Shopify MCP Server is ready!** Once authentication is complete, you can use your MCP server URL with any MCP-compatible client.
        </Check>
      </Step>
      
    </Steps>
  </Tab>
  
  <Tab title="UI">
    <Steps>
      <Step title="Open Dashboard">
        Go to your [Dashboard](https://www.mcp-handle.dev/home/mcp-servers).
      </Step>
      
      <Step title="Choose Shopify">
        Select Shopify from the list of available integrations.
      </Step>
      
      <Step title="Authenticate">
        Complete the OAuth flow to connect your Shopify account.
      </Step>
      
      <Step title="Use in your app">
        Copy the MCP endpoint URL and add it to your MCP-supported client (Claude Desktop, Cursor, VS Code, etc.).
      </Step>
    </Steps>
  </Tab>
  
  <Tab title="Open Source">
    <Steps>
      <Step title="Clone repository (for developers to build from source)">
        ```bash
        git clone https://github.com/WeatherPal-AI/MCP-handle
        cd mcp_handle/mcp_servers/shopify
        ```
      </Step>
      
      <Step title="Run with Docker">
        ```bash
        # Pull the Docker image
        docker pull ghcr.io/weatherpal-ai/shopify-mcp-server:latest
        
        # Run with OAuth support (requires MCP Handle API key)
        docker run -p 5000:5000 \
          -e MCP_HANDLE_API_KEY=$MCP_HANDLE_API_KEY \
          ghcr.io/weatherpal-ai/shopify-mcp-server:latest
        
        # Or run with manual API key
        docker run -p 5000:5000 \
          -e AUTH_DATA='{"access_token":"your_shopify_api_key"}' \
          ghcr.io/weatherpal-ai/shopify-mcp-server:latest
        ```
      </Step>
      
      <Step title="Configure MCP client">
        ```json
        {
          "mcpServers": {
            "shopify": {
              "url": "http://localhost:5000/mcp/"
            }
          }
        }
        ```
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Available Tools

<Tip>
With our progressive discovery approach, MCP Handle System is capable of enabling all tools for Shopify. Please use the [get_tools](https://www.mcp-handle.dev/docs/api-reference/mcp-server/get-tools) API for more details. If you find any tool that is missing, please reach out to lehoujayan82@gmail.com.
</Tip>

## Next Steps

<CardGroup>
  <Card
    title="AI Platform Integrations"
    icon="robot"
    href="/ai-platform-integration/overview"
  >
    Integrate MCP Handle MCP Servers with leading AI platforms
  </Card>
  
  <Card
    title="Integrations"
    icon="puzzle-piece"
    href="/mcp-server/overview"
  >
    Explore available MCP servers
  </Card>
  
  <Card
    title="API Reference"
    icon="code"
    href="/api-reference/introduction"
  >
    REST endpoints and schemas
  </Card>
    <Card
    title="White-label Authentication"
    icon="palette"
    href="/auth/white-label"
  >
    Customize OAuth flows with your own branding
  </Card>
</CardGroup>