---
title: 'Gong'
description: 'Connect AI agents to Gong for sales conversation intelligence and call analytics'
---

<Info>
  **Prerequisites**
  Before you begin, [create an account](https://www.mcp-handle.dev/home/api-keys) and get your API Key.
</Info>

## Getting started

Connect to Gong to access sales conversation intelligence, analyze call transcripts, and extract insights from sales meetings through AI agents.

<Tabs>
  <Tab title="API">
    <Steps>
      <Step title="Install the SDKs (optional)">
        <CodeGroup>
          ```bash pip
          pip install mcp_handle
          ```
          ```bash npm
          npm install mcp_handle
          ```
        </CodeGroup>
      </Step>
      
      <Step title="Create a Strata MCP Server with Gong">
        <CodeGroup>
          ```python Python
          from mcp_handle import MCP Handle
          from mcp_handle.types import McpServerName
          
          mcp_handle_client = MCP Handle(api_key="YOUR_API_KEY")
          
          # Create a Strata MCP server with Gong
          response = mcp_handle_client.mcp_server.create_strata_server(
              servers=[McpServerName.GONG],
              user_id="user123"
          )
          ```
          
          ```typescript TypeScript
          import { MCP HandleClient, MCP Handle } from 'mcp_handle';
          
          const mcp_handle = new MCP HandleClient({ apiKey: process.env.MCP_HANDLE_API_KEY! });
          
          // Create a Strata MCP server with Gong
          const response = await mcp_handle.mcpServer.createStrataServer({
              servers: [MCP Handle.McpServerName.Gong],
              userId: "user123"
          });
          ```
          
          ```bash cURL
          curl -X POST "https://api.mcp-handle.dev/mcp-server/strata/create" \
            -H "Authorization: Bearer YOUR_API_KEY" \
            -H "Content-Type: application/json" \
            -d '{
              "servers": ["Gong"],
              "userId": "user123"
            }'
          ```
        </CodeGroup>

        <Card title="API Reference" icon="magnifying-glass" href="/api-reference/strata/create" horizontal>
          Full Strata API endpoints
        </Card>
      </Step>
      
      <Step title="Authenticate with API Key">
        <Info>
          You can get your Gong API key from your Gong admin settings. Contact your Gong administrator for access.
        </Info>
        
        <CodeGroup>
          ```python Python
          mcp_handle_client.mcp_server.set_strata_auth(
              strata_id=response.strata_id,
              server_name=McpServerName.GONG,
              auth_data={
                  "api_key": "YOUR_GONG_API_KEY"
              }
          )
          ```
          
          ```typescript TypeScript
          await mcp_handle.mcpServer.setStrataAuth({
              strataId: response.strataId,
              serverName: MCP Handle.McpServerName.Gong,
              authData: {
                  api_key: "YOUR_GONG_API_KEY"
              }
          });
          ```
          
          ```bash cURL
          curl -X POST "https://api.mcp-handle.dev/mcp-server/strata/set-auth" \
            -H "Authorization: Bearer YOUR_MCP_HANDLE_API_KEY" \
            -H "Content-Type: application/json" \
            -d '{
              "strataId": "YOUR_STRATA_ID",
              "serverName": "Gong",
              "authData": {
                "api_key": "YOUR_GONG_API_KEY"
              }
            }'
          ```
        </CodeGroup>
        
        <Check>
          🎉 **Your Gong MCP Server is ready!** Once authentication is complete, you can use your MCP server URL with any MCP-compatible client.
        </Check>
      </Step>
      

    </Steps>
  </Tab>
  
  <Tab title="UI">
    <Steps>
      <Step title="Open Dashboard">
        Go to your [Dashboard](https://www.mcp-handle.dev/home/mcp-servers).
      </Step>
      
      <Step title="Choose Gong">
        Select Gong from the list of available integrations.
      </Step>
      
      <Step title="Configure API Key">
        Enter your Gong API key to enable conversation intelligence access.
      </Step>
      
      <Step title="Use in your app">
        Copy the MCP endpoint URL and add it to your MCP-supported client (Claude Desktop, Cursor, VS Code, etc.).
      </Step>
    </Steps>
  </Tab>
  
  <Tab title="Open Source">
    <Steps>
      <Step title="Clone repository (for developers to build from source)">
        ```bash
        git clone https://github.com/WeatherPal-AI/MCP-handle
        cd mcp_handle/mcp_servers/gong
        ```
      </Step>
      
      <Step title="Run with Docker">
        ```bash
        # Pull the Docker image
        docker pull ghcr.io/weatherpal-ai/gong-mcp-server:latest
        
        # Run with API key support (requires MCP Handle API key)
        docker run -p 5000:5000 \
          -e MCP_HANDLE_API_KEY=$MCP_HANDLE_API_KEY \
          ghcr.io/weatherpal-ai/gong-mcp-server:latest
        ```
      </Step>
      
      <Step title="Configure MCP client">
        ```json
        {
          "mcpServers": {
            "gong": {
              "url": "http://localhost:5000/mcp/"
            }
          }
        }
        ```
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Available Tools

<Tip>
With our progressive discovery approach, MCP Handle System is capable of enabling all tools for Gong. Please use the [get_tools](https://www.mcp-handle.dev/docs/api-reference/mcp-server/get-tools) API for more details. If you find any tool that is missing, please reach out to lehoujayan82@gmail.com.
</Tip>

## Next Steps

<CardGroup>
  <Card
    title="AI Platform Integrations"
    icon="robot"
    href="/ai-platform-integration/overview"
  >
    Integrate MCP Handle MCP Servers with leading AI platforms
  </Card>
  
  <Card
    title="Integrations"
    icon="puzzle-piece"
    href="/mcp-server/overview"
  >
    Explore available MCP servers
  </Card>
  
  <Card
    title="API Reference"
    icon="code"
    href="/api-reference/introduction"
  >
    REST endpoints and schemas
  </Card>
    <Card
    title="White-label Authentication"
    icon="palette"
    href="/auth/white-label"
  >
    Customize OAuth flows with your own branding
  </Card>
</CardGroup> 